# pptx-tool

Batch convert PowerPoint (.pptx) files to Markdown and PDF.

## Usage

Place the binary in a directory containing `.pptx` files and run it:

```
pptx-tool
```

Each `.pptx` file produces a `.md` and `.pdf` alongside it.

## Build

```
cargo build --release
```

## License

MIT
